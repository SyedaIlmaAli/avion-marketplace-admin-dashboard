import Home from "@/components/HomePage";

const page = async () => {
//     const {getUser} = getKindeServerSession();
// const user = await getUser();
// console.log(user);

  return (
    <div>
      <Home/>
    </div>
  )
}

export default page
